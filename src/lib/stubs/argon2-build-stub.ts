export async function hash() {
  throw new Error('argon2 build stub should not run at runtime');
}

export async function verify() {
  throw new Error('argon2 build stub should not run at runtime');
}

export default { hash, verify };
